console.log('check')

var $ = jQuery;

$('.hfo-slider').slick({
    slidesToShow: 4,
    slidesToScroll: 4,
    // infinite: true,
    arrows: true,
    // dots: true,
    // appendArrows: '',
    // dotsPosition: 'bottom',
  });
